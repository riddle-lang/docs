# 数据与抽象

程序不仅要计算，还要组织数据和行为。Riddle 使用结构体描述数据，用 trait 描述能力，用 impl 把能力实现到具体类型上。

这一部分会介绍：

- 如何用 `struct` 定义数据；
- 如何用 `trait` 定义一组行为；
- 如何用 `impl` 为类型添加函数和实现 trait；
- Riddle 为什么支持在一个 `impl` 块中同时实现多个 trait。

如果你熟悉 Rust，可以把这一部分理解成对 Rust 数据抽象模型的简化和改进。
如果你不熟悉 Rust，也可以只把它看成一种把数据和能力组织起来的方式。
