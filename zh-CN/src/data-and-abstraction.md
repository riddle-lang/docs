# 数据建模与抽象

掌握所有权后，就可以用 Riddle 的类型与模块机制组织较大的程序。本部分按“先表示数据，再定义行为，最后划分边界”的顺序展开：

1. 结构体把相关字段组合成一个类型；
2. 枚举和模式表达一组互斥的数据形状；
3. trait 声明共享能力，`impl` 为具体类型提供行为；
4. 模块、可见性、`use` 和包依赖划分名字与 API 边界。

Riddle 当前没有 Kotlin 式类继承，也没有 Rust 式动态 trait object。这里的抽象都通过静态类型、trait bound 和单态化完成。
