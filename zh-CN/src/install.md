# 安装 Riddle 环境

因为 Riddle 是使用 Rust 编写的，所以在编译之前请[安装 Rust](https://course.rs/first-try/installation.html)。

Rust 安装完成之后，请找个好地方（类似你的专用的 Code 目录）执行下述命令：
```bash
git clone https://github.com/riddle-lang/riddle --depth=1
cd riddle
cargo build
```
然后你就得到了一个新鲜出炉的 riddlec

